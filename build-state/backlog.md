# OlcPanel Build Backlog (WBS из доков v2.0/v2.1)
# Single Source of Truth: /root/olcrtc/docs/Architecture\ olcRTC\ panel/OlcPanel_* + OlcPanel_Анализ_Marzban_3xui.md §4/5/7/8
# Поля: id | title | stage(1/2/3) | status(todo/in_progress/blocked/review/done) | depends_on[] | parallel(yes/no) | acceptance_criteria | proof(link/test/log) | owner(self/hermes/sub-N) | notes
# Обновляется на каждом шаге цикла. Идемпотентно: проверяй статус + фактическое состояние перед стартом.

## ЭТАП 1 — Монетизация + Фундамент (делать первым; приоритет по Полез/Применимость при низкой Сложн из анализа §7)
# Источник: анализ §8 "С чего начать", §7 таблица, 00_Введение Решения 3-4, 02_Модель, 03_Сервисы (LKG, writer, Coordinator skeleton), 05_Бизнес (авто-блок)

1 | LKG в Gate (internal/authz/authz.go) | 1 | done | [] | no | Поля lkgAllow/lkgDeny/lkgValidAt/loadErrors; Config.FailMode (lkg|open|closed, прод=lkg), LkgMaxAge; валидация схемы JSON + version-reject (не сбрасывать LKG при откате); cold-start closed+alert при отсутствии prior good; тест AZ-07 ("битый/пропавший файл в lkg НЕ расширяет allow"); интеграция с session.go + config.go; go vet + unit; build smoke. | PASS: go test ./internal/authz -run 'LKG|Cold|Rollback|Missing' (AZ-07 + version monotonic + cold closed + bad file keeps LKG; legacy open tests with explicit FailMode=open); go vet clean; /tmp/olcrtc-lkg-test-* builds; logs show "using LKG (errors=..)" on corrupt/rollback/missing, no expansion of allow. See run.log 2026-06-14 entries + test output in terminal log. Proof in /root/olcrtc/internal/authz/authz_test.go (new TestLKG* cases) + authz.go refresh/Allowed. | self | Первая задача по промту §12 и анализу 5.2 ЗАВЕРШЕНА. wiring обновлены (config.go Authz struct + session.Config + gate creation). Не включать mode allowlist. Panel writer всё ещё хардкодит v=1 — следующая. LKG активен по умолчанию для allowlist/denylist.
2 | authz_writer: монотонный version + authz_state таблица | 1 | done | [1] | no | Атомарная запись (tmp+fsync+rename, уже в writer.py); инкремент version из authz_state (singleton или per-server); payload всегда с version++ ; writer вызывается при create/extend/block/scheduler; тест атомарности + version-моно. | pytest на writer; dry-run; smoke curl /api/sub + x-hwid → версия растёт в authz.json | self | ЗАВЕРШЕНО в tick 2026-06-14. Доказательства: 1. Миграция runtime в main.py (CREATE TABLE IF NOT EXISTS authz_state + INSERT OR IGNORE seed). 2. models.py: AuthzState (id PK, version, allow_count, last_writer). 3. services/authz_writer.py: теперь читает/бампает из AuthzState (commit версии перед файлом), payload с version+allow_count; runtime config.OLCRTC...; atomic tempfile сохранён. 4. Тесты (venv python): таблица создана, seed row, temp monotonic bump (v3→v4), LIVE smoke writer call на реальной БД+authz.json → version 5, state version=5, last_writer=authz_writer, allow_count обновлён. Backups pre-edit (/root/olcrtc-backups/*). DB эволюционная (не сломаны 6 клиентов + device_binding). Callers (routers/clients.py, subscription.py, scheduler.py) unchanged — автоматически используют новый writer. Полный цикл PLAN/DECOMPOSE/EXEC/TEST/VERIFY/CRITIQUE/RECORD. См. run.log + /tmp/grok_tick2_test.py. Next: task3 (Alembic + user_devices/servers full). 
3 | Схема БД v2.0 + миграции (Alembic) | 1 | done | [2] | no | user_devices (или эволюция device_binding: device_id, server_id, is_active, last_used), servers (id, name, host, api_base, status, priority, fail_mode, expected/applied_authz_version, lkg_valid_at, load_errors, last_health_at), authz_state (version монотонный), server_id в subscriptions/clients или эквиваленте (subscriptions? current client+device); миграции + rollback; LEGACY/DORMANT traffic/configs сохранить; server_settings.authz_* ; dry-run import, тесты миграций. | runtime run_migrations (CREATE/ALTER IF NOT EXISTS + seed + backfill) + python verify: server table+health cols present, device_binding +server_id/is_active, default server seeded (id=1), existing devices backfilled server=1/is_active=1; crud.get_active_device_ids(db, server_id=?) + is_active filter; bind default server; writer payload now includes mode/deny per 02; version monotonic bump live (5->6); /tmp/task3_evo_verify.py full output; no breakage to 6 clients +1 device; panel import/main starts clean. See run.log 2026-06-14 task#3 + backups 20260614T082638Z. | self | Эволюционно (не ломает live Phase1 device_binding + client + /api/sub bind flow). server skeleton + LKG health поля заложены с Этапа 1 (фундамент #5). Расхождение имён (client/device_binding vs canonical clients/user_devices) — задокументировать/предложить правку в 02 позже; текущая модель остаётся источником для writer. Готово к #4 (авто-блок с is_active) и #5 (Coordinator по server_id). Полный цикл + guardrails соблюдены.
4 | Авто-блок по сроку (Cron/APScheduler → authz_writer) + аудит | 1 | done | [1,2,3] | no | APScheduler джоба сканирует expired subscriptions/clients с expiry; soft-block: is_active=false в devices + write_authz_file (version++); аудит (audit_log) всех write_authz / block / extend; Swagger на /api ; тест: создать с коротким expiry → через джобу в allow нет, authz version растёт. | scheduler тест; интеграц. smoke: curl create client short-expire → подождать → /api/clients показывает expired, authz.json без device; audit entries | self | ЗАВЕРШЕНО в tick 2026-06-14. Доказательства: scheduler.py обновлён (check_expiry_dates теперь ставит client.status=expired + device_binding.is_active=0 для устройств клиента, логирует в audit_log "client.expired"/"device.soft_block", вызывает write_authz_file). Аналогично для traffic block. Добавлены: models.AuditLog, runtime migration в main.py (CREATE IF NOT audit_log), crud.log_audit(). Полный тест (/tmp/test_task4_auto_block.py под venv): создан клиент с прошлой expiry + bind device → force check_expiry_dates() → status=expired, все devices is_active=0, устройство отсутствует в get_active_device_ids(), authz version bumped (6→7), audit_log содержит записи. Live данные (6 клиентов) не пострадали. Backups pre-edit. Herm es restart + post smoke (APScheduler подхватил новый код, audits свежие). Соответствует 02/05 + acceptance. Next: Task5 (ServerRegistry+Coordinator skeleton, можно параллельно). Полный цикл + guardrails. См. run.log + test script.
5 | Скелет флота: ServerRegistry, Coordinator + LocalFilePusher (no-op для 1 сервера) | 1 | todo | [3] | yes (параллельно с 4 если независимо) | ServerRegistry (CRUD servers, health поля); IssuancePolicy (pick server_id по priority); MultiServerAuthzCoordinator.on_change(server_id) → compute slice from DB (по user_devices.server_id) → LocalFilePusher (вызывает текущий writer для default server, no-op multi); feature flag multi_server_authz=off по умолчанию; контракт payload version+updated_at+allow+deny. Поведение single-server идентично текущему. | unit/integration на coordinator (dry compute + write); /api/health включает applied etc для default; no change in prod traffic при single. | self | Закладываем с Этапа 1 (анализ 4.8, 00 Реш 3). Реальный rsync push — Этап 2. health-poll self для single.

## ЭТАП 2 — Операционка
6 | Доставка authz (Pusher A: rsync+trigger) + health-poll | 2 | todo | [5] | no | ServerAuthzPusher абстракция + RsyncPusher (реализация A); trigger на сервере (HTTP или path unit → атомарный rename в target authz.json); ServerHealthService poll /health (live?, applied_version, lkg_valid_at, load_errors); обновление servers.* ; алерт на push-fail / расхождение. | e2e: push to simulated second "server" dir; health обновляет applied==expected; failure path → audit + log | hermes/sub | + Telegram в 7.
7 | Telegram-уведомления (вкл. push-failure, expiry warn) + напоминания | 2 | todo | [4,6] | yes | NotificationService (Telegram bot token из .env); события: client create/extend/block-by-expiry, push failed (per server), server down, warn N дней (server_settings.warn_days_before); APScheduler вызывает. | Тест dry send + реальный в TG (если токен); inbox/outbox bridge для тестов; дашборд показывает alerts | self/hermes | 3x-ui/Marzban донор.
8 | Авто-бэкап БД (+authz.json, server.yaml) + ротация | 2 | todo | [] | no | Ежедневный cron/systemd-timer: sqlite3 .backup + cp authz.json + server.yaml + tar.gz в backups/ с retention 7-30d; опционально TG; скрипт + timer unit. | backup run → файлы в backups/ с датой; restore dry-run; disk не растёт. | hermes | Уже частично (build-state, 07_Инфраструктура).
9 | Безопасность входа в панель (fail2ban + 2FA/секретный путь) | 2 | todo | [] | no | fail2ban для nginx/8000; TOTP 2FA (pyotp + qrcode в login); секретный путь /panel-SECRET/ вместо / (nginx rewrite или app mount); rate limit усилить. | login brute симуляция банится; 2FA flow тест; secret path 404 на /api без него. | hermes | 3x-ui донор, повышается при multi.
10 | Шаблоны страницы подписки + /api/sub/{token} улучшения | 2 | todo | [3] | no | Механика Marzban-style (User-Agent routing, HTML fallback page для subs, красивый render olcrtc:// URI); текущий /api/sub bind_device + write_authz; улучшить UX (QR, copy, expiry info). | curl с разными UA → разный/красивый ответ; тест bind device → allow в json. | self | Не тащить чужие форматы (v2ray etc) — только olcrtc:// (см uri.md, sub.md).

## ЭТАП 3 — Масштабирование / Зрелость
11 | Pusher B (signed HTTP / mTLS + version-reject на сервере) | 3 | todo | [6] | no | HttpPusher (B); /internal/authz/update endpoint на olcrtc (validate sig/version > applied, atomic write, 200 только после); server side mTLS or token; coordinator использует. | Интеграц тест: signed push accepted, old version rejected (LKG preserved); health applied обновляется. | sub/hermes | После A стабильно.
12 | Мониторинг флота на дашборде (applied_version, lkg age, load_errors, split-brain alerts) | 3 | todo | [6,11] | yes | /api/dashboard + UI показывает per-server health (из servers table + health-poll); "Требуют внимания" при applied < expected / load_errors>0 / lkg stale; Chart.js. | UI smoke + данные с 1-2 "серверов"; alert в TG. | self | 06_UI + 03.
13 | Миграция устройств между серверами (порядок add-new then remove-old) | 3 | todo | [5,11] | no | В IssuancePolicy + Coordinator: при смене server_id на device → on_change(Y) first (add allow на new), затем on_change(X) (remove со старого); health confirm; временно allowed на обоих до подтверждения (fail-safe). | Тест: device migrate X→Y → allow на Y прежде чем убран с X; session на старом рвётся после. | self | Анализ 4.7.
14 | RBAC / реселлеры / multi-admin | 3 | todo | [] | no | admins таблица + роли; reseller видит только своих clients; sudo для owner. | CRUD admin с ограничениями; тест reseller не видит чужих. | self | Анализ 3.8.
15 | Webhook → автоплатежи | 3 | todo | [7] | no | WebhookService: POST на configurable URL при events (payment, extend, block); signature; тестовый endpoint. | Dry webhook + verify sig; интеграция с биллингом позже. | self | Для Этап 3+.

## Инфраструктура / Крон / Общее (параллельно где возможно)
16 | Крон-тик + lock + resume + scheduled checks (soak, backup, alert LKG stale) | 1 | done (partial) | [] | no | tick.sh + flock + RESUME_PROMPT; crontab entry; ежедневный backup timer; health-soak туннеля; alert load_errors or lkg age. | Крон тик работает (run.log показывает START/END); lock предотвращает дубли; backups ротируются. | hermes | tick.sh / build-state уже есть (из bootstrap); проверить crontab -l и добавить если нет. 
17 | Git hygiene + CI smoke (для Go + panel) | 1 | todo | [] | no | feat/* ветки для Go; коммиты логические; panel repo push; pre-commit vet/test; no secrets. | git status clean; push to asadulakurbanov4-web/olcRTC-panel (panel); go test on CI if any. | self | Не пуш broken в main.
18 | Финальный отчёт + процедура активации гейта | 3 | todo | [1-15] | no | /root/olcrtc/build-state/FINAL_REPORT.md: что сделано + доказательства (тесты, логи, health), что требует человека (активация mode=allowlist после bind owner device + verify allow in json), как откатить (mv backups), sync docs. | Отчёт существует, все этапы closed с доказ-вами; туннель жив (selftest OK). | self | Критерий завершения §11 промта.

## Блокеры / Риски (живые)
- Диск ~100% (мониторить, чистить артефакты сборок /tmp/olcrtc* / старые бэкапы по ротации). Текущий: 86% после чистки.
- Нет sqlite3 bin (использовать python sqlite3 для инспекции).
- Go bin: /usr/local/go/bin/go (не в PATH по умолчанию в некоторых shell).
- Текущая панель схема (client/device_binding) расходится с канонической 02 — эволюционно мигрировать, не ломать running (clients active, device bindings существуют).
- authz.json allow пустой — НЕ флипать mode=allowlist (владелец/тест-dev не попадут, туннель сломается для живых).
- Нет реального Hermes вызова в этом логе — при тяжёлых шагах (go build/test full, restart, deploy) вызывать `hermes -z "<task with paths+criteria>"` и верифицировать вывод.
- Panel /opt НЕ git-репо для деплоя (хотя .git есть); изменения в /root/olcrtc/docs или panel source (приватный gh asadulakurbanov4-web/olcRTC-panel) — sync отдельно.

Статус обновлён: 2026-06-14. Первый тик после получения полного автономного промта. Начат 1 (LKG).
