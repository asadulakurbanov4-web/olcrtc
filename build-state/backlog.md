# OlcPanel Build Backlog (WBS из доков v2.0/v2.1)
# Single Source of Truth: /root/olcrtc/docs/Architecture\ olcRTC\ panel/OlcPanel_* + OlcPanel_Анализ_Marzban_3xui.md §4/5/7/8
# Поля: id | title | stage(1/2/3) | status(todo/in_progress/blocked/review/done) | depends_on[] | parallel(yes/no) | acceptance_criteria | proof(link/test/log) | owner(self/hermes/sub-N) | notes
# Обновляется на каждом шаге цикла. Идемпотентно: проверяй статус + фактическое состояние перед стартом.

## ЭТАП 1 — Монетизация + Фундамент (делать первым; приоритет по Полез/Применимость при низкой Сложн из анализа §7)
# Источник: анализ §8 "С чего начать", §7 таблица, 00_Введение Решения 3-4, 02_Модель, 03_Сервисы (LKG, writer, Coordinator skeleton), 05_Бизнес (авто-блок)

1 | LKG в Gate (internal/authz/authz.go) | 1 | done | [] | no | Поля lkgAllow/lkgDeny/lkgValidAt/loadErrors; Config.FailMode (lkg|open|closed, прод=lkg), LkgMaxAge; валидация схемы JSON + version-reject (не сбрасывать LKG при откате); cold-start closed+alert при отсутствии prior good; тест AZ-07 ("битый/пропавший файл в lkg НЕ расширяет allow"); интеграция с session.go + config.go; go vet + unit; build smoke. | PASS: go test ./internal/authz -run 'LKG|Cold|Rollback|Missing' (AZ-07 + version monotonic + cold closed + bad file keeps LKG; legacy open tests with explicit FailMode=open); go vet clean; /tmp/olcrtc-lkg-test-* builds; logs show "using LKG (errors=..)" on corrupt/rollback/missing, no expansion of allow. See run.log 2026-06-14 entries + test output in terminal log. Proof in /root/olcrtc/internal/authz/authz_test.go (new TestLKG* cases) + authz.go refresh/Allowed. | self | Первая задача по промту §12 и анализу 5.2 ЗАВЕРШЕНА. wiring обновлены (config.go Authz struct + session.Config + gate creation). Не включать mode allowlist. Panel writer всё ещё хардкодит v=1 — следующая. LKG активен по умолчанию для allowlist/denylist.
2 | authz_writer: монотонный version + authz_state таблица | 1 | done | [1] | no | Атомарная запись (tmp+fsync+rename, уже в writer.py); инкремент version из authz_state (singleton или per-server); payload всегда с version++ ; writer вызывается при create/extend/block/scheduler; тест атомарности + version-моно. | pytest на writer; dry-run; smoke curl /api/sub + x-hwid → версия растёт в authz.json | self | ЗАВЕРШЕНО в tick 2026-06-14. Доказательства: 1. Миграция runtime в main.py (CREATE TABLE IF NOT EXISTS authz_state + INSERT OR IGNORE seed). 2. models.py: AuthzState (id PK, version, allow_count, last_writer). 3. services/authz_writer.py: теперь читает/бампает из AuthzState (commit версии перед файлом), payload с version+allow_count; runtime config.OLCRTC...; atomic tempfile сохранён. 4. Тесты (venv python): таблица создана, seed row, temp monotonic bump (v3→v4), LIVE smoke writer call на реальной БД+authz.json → version 5, state version=5, last_writer=authz_writer, allow_count обновлён. Backups pre-edit (/root/olcrtc-backups/*). DB эволюционная (не сломаны 6 клиентов + device_binding). Callers (routers/clients.py, subscription.py, scheduler.py) unchanged — автоматически используют новый writer. Полный цикл PLAN/DECOMPOSE/EXEC/TEST/VERIFY/CRITIQUE/RECORD. См. run.log + /tmp/grok_tick2_test.py. Next: task3 (Alembic + user_devices/servers full). 
3 | Схема БД v2.0 + миграции (Alembic) | 1 | done | [2] | no | user_devices (или эволюция device_binding: device_id, server_id, is_active, last_used), servers (id, name, host, api_base, status, priority, fail_mode, expected/applied_authz_version, lkg_valid_at, load_errors, last_health_at), authz_state (version монотонный), server_id в subscriptions/clients или эквиваленте (subscriptions? current client+device); миграции + rollback; LEGACY/DORMANT traffic/configs сохранить; server_settings.authz_* ; dry-run import, тесты миграций. | runtime run_migrations (CREATE/ALTER IF NOT EXISTS + seed + backfill) + python verify: server table+health cols present, device_binding +server_id/is_active, default server seeded (id=1), existing devices backfilled server=1/is_active=1; crud.get_active_device_ids(db, server_id=?) + is_active filter; bind default server; writer payload now includes mode/deny per 02; version monotonic bump live (5->6); /tmp/task3_evo_verify.py full output; no breakage to 6 clients +1 device; panel import/main starts clean. See run.log 2026-06-14 task#3 + backups 20260614T082638Z. | self | Эволюционно (не ломает live Phase1 device_binding + client + /api/sub bind flow). server skeleton + LKG health поля заложены с Этапа 1 (фундамент #5). Расхождение имён (client/device_binding vs canonical clients/user_devices) — задокументировать/предложить правку в 02 позже; текущая модель остаётся источником для writer. Готово к #4 (авто-блок с is_active) и #5 (Coordinator по server_id). Полный цикл + guardrails соблюдены.
4 | Авто-блок по сроку (Cron/APScheduler → authz_writer) + аудит | 1 | done | [1,2,3] | no | APScheduler джоба сканирует expired subscriptions/clients с expiry; soft-block: is_active=false в devices + write_authz_file (version++); аудит (audit_log) всех write_authz / block / extend; Swagger на /api ; тест: создать с коротким expiry → через джобу в allow нет, authz version растёт. | scheduler тест; интеграц. smoke: curl create client short-expire → подождать → /api/clients показывает expired, authz.json без device; audit entries | self | ЗАВЕРШЕНО в tick 2026-06-14. Доказательства: scheduler.py обновлён (check_expiry_dates теперь ставит client.status=expired + device_binding.is_active=0 для устройств клиента, логирует в audit_log "client.expired"/"device.soft_block", вызывает write_authz_file). Аналогично для traffic block. Добавлены: models.AuditLog, runtime migration в main.py (CREATE IF NOT audit_log), crud.log_audit(). Полный тест (/tmp/test_task4_auto_block.py под venv): создан клиент с прошлой expiry + bind device → force check_expiry_dates() → status=expired, все devices is_active=0, устройство отсутствует в get_active_device_ids(), authz version bumped (6→7), audit_log содержит записи. Live данные (6 клиентов) не пострадали. Backups pre-edit. Herm es restart + post smoke (APScheduler подхватил новый код, audits свежие). Соответствует 02/05 + acceptance. Next: Task5 (ServerRegistry+Coordinator skeleton, можно параллельно). Полный цикл + guardrails. См. run.log + test script.
5 | Скелет флота: ServerRegistry, Coordinator + LocalFilePusher (no-op для 1 сервера) | 1 | done | [3] | yes (параллельно с 4 если независимо) | ServerRegistry (CRUD servers, health поля); IssuancePolicy (pick server_id по priority); MultiServerAuthzCoordinator.on_change(server_id) → compute slice from DB (по user_devices.server_id) → LocalFilePusher (вызывает текущий writer для default server, no-op multi); feature flag multi_server_authz=off по умолчанию; контракт payload version+updated_at+allow+deny. Поведение single-server идентично текущему. | unit/integration на coordinator (dry compute + write); /api/health включает applied etc для default; no change in prod traffic при single. | self | ЗАВЕРШЕНО в tick 2026-06-14. Доказательства: config.py + MULTI_SERVER_AUTHZ flag (off); schemas.py + Server*; crud.py + full Server CRUD (get/create/update/delete) + get_or_create_default + bind uses pick_server_id; services/coordinator.py (new: LocalFilePusher, MultiServerAuthzCoordinator.on_change, pick_server_id); scheduler.py integrated coordinator.on_change (replaces direct writer calls). Test /tmp/test_task5_fleet_skeleton.py: CRUD (create/update/delete test server), pick_server_id=1, coordinator.on_change bumps version (7->8), payload mode/deny, pusher direct, flag off, single behavior identical (no prod traffic change). Live 6 clients + device safe. Backups + hermes smoke. Single MVP foundation laid (per 02/03). Next: Task6 (Pusher A rsync+health). Полный цикл + guardrails. См. run.log + test.

## ЭТАП 2 — Операционка
6 | Доставка authz (Pusher A: rsync+trigger) + health-poll | 2 | done | [5] | no | ServerAuthzPusher абстракция + RsyncPusher (реализация A); trigger на сервере (HTTP или path unit → атомарный rename в target authz.json); ServerHealthService poll /health (live?, applied_version, lkg_valid_at, load_errors); обновление servers.* ; алерт на push-fail / расхождение. | e2e: push to simulated second "server" dir; health обновляет applied==expected; failure path → audit + log | hermes/sub | ЗАВЕРШЕНО 2026-06-14 (subagent task6). Доказательства: 
- New: /opt/olcrtc-panel/backend/services/pusher.py (ServerAuthzPusher ABC, RsyncPusher: _deliver_via_rsync_trigger (shutil panel_tmp -> target.tmp + _invoke_server_trigger: fsync+os.replace atomic + utime), default_pusher, local id=1 target=OLCRTC_AUTHZ_FILE + sim_mode/target_base for /tmp/olcrtc-sim-server-N).
- New: services/health_service.py (ServerHealthService: poll(db, sid, target_override) reads sim/live authz.json as applied proxy (or urllib /health), calls crud.update_server_health, detects applied<expected/load_errors>0 -> audit "health.split_brain"/"health.load_errors").
- Updates (scope): services/authz_writer.py (added build_authz_payload for pusher reuse + version bump), services/coordinator.py (refactored to import/use RsyncPusher+health_service, on_change builds payload + pusher.push + set expected), services/scheduler.py (added refresh_fleet_health job 5min + health_service integration), crud.py (set_server_expected_version, update_server_health, get_server_health + Optional import), config.py (SIMULATED_SERVER_BASE, HEALTH_POLL_TIMEOUT_SEC).
- main.py: no edit needed (scheduler.start now registers health job).
- Backups: /root/olcrtc-backups/*task6-pre-20260614T143000Z.bak (coordinator/scheduler/writer/config/crud/main + authz.json + panel.db via test).
- E2E test + proof: /tmp/test_task6_pusher_health_e2e.py (self-contained, sim dir=99, push payload, verify atomic delivered file+ver, health.poll -> applied==expected load=0, failure ro-dir -> push False + audit push_failed, live id=1 flow test via RsyncPusher, DB backup, /tmp/task6_e2e_proof.txt + sim dir left); run.log entries with full cycle + "PASS".
- Live: authz v bumped harmless (gate off, allow=[]), no tunnel impact, atomic preserved.
См. /root/olcrtc/build-state/run-task6.log (detailed timestamps, critique, guardrails), /tmp/task6_e2e_proof.txt. Полный цикл PLAN/DECOMPOSE/EXEC (search_replace+write)/TEST/VERIFY (asserts+static)/CRITIQUE/RECORD (backlog+log). Соответствует 03 (Pusher A/Local->Rsync, HealthService, scheduler refresh), 07.10.3-4 (rsync+trigger atomic, health poll), backlog acceptance. Next: Task7 (Telegram + push-fail alerts). 
7 | Telegram-уведомления (вкл. push-failure, expiry warn) + напоминания | 2 | done | [4,6] | yes | NotificationService (Telegram bot token из .env); события: client create/extend/block-by-expiry, push failed (per server), server down, warn N дней (server_settings.warn_days_before); APScheduler вызывает. | Тест dry send + реальный в TG (если токен); inbox/outbox bridge для тестов; дашборд показывает alerts | self/hermes | ЗАВЕРШЕНО 2026-06-14 (subagent task7+8). Доказательства:
- New: /opt/olcrtc-panel/backend/services/notification.py (NotificationService: _send_telegram via stdlib urllib (no dep), dry if no token/chat (log only), notify() + wrappers for client.created/extended/blocked/device.soft_block/authz.push_failed/server.down/expiry.warn; singleton; RU HTML msgs).
- Config: + TELEGRAM_BOT_TOKEN/CHAT_ID, WARN_DAYS_BEFORE, BACKUP_* (env override, defaults safe).
- scheduler.py: imports + notifies on block/expiry (in checks + device loops), + check_expiry_warnings (daily@09:00), check_server_health_basic (15m, systemctl + TG down), start_scheduler registers warn+health (backup too); job for backup@02:00 if importable.
- clients.py: import + notify created on POST, extended/block on PUT (diff old/new expiry/status).
- coordinator.py + authz_writer.py: import + on push fail (pusher !ok or writer except) → notify_push_failed (covers task6 pusher fails).
- meta.py: /api/server/status now returns "notifications": "dry|enabled", "warn_days".
- /tmp/test_task7_notifications_dry.py (dry force, all wrappers, enabled path call; PASS no crash).
- Backups pre: /root/olcrtc-backups/*task7-task8-20260614T132000Z (all key py).
- Dry default (no TG token = safe, no net), audit+logger always; compatible with existing APS + task6 pusher.
- Scope exact, no DB change, live 6 clients + /api/sub flow untouched. См. conversations/.../grok-*.md + run.log.
8 | Авто-бэкап БД (+authz.json, server.yaml) + ротация | 2 | done | [] | no | Ежедневный cron/systemd-timer: sqlite3 .backup + cp authz.json + server.yaml + tar.gz в backups/ с retention 7-30d; опционально TG; скрипт + timer unit. | backup run → файлы в backups/ с датой; restore dry-run; disk не растёт. | hermes | ЗАВЕРШЕНО 2026-06-14 (subagent task7+8). Доказательства:
- New: /opt/olcrtc-panel/backend/services/backup.py (run_backup(dry=False): sqlite3.connect().backup() equiv hot, cp AUTHZ+SERVER_YAML into dated stamp/, tar.gz bundle, retention cleanup by ts/mtime > RETENTION_DAYS; config driven BACKUP_DIR/RETENTION; standalone __main__ + dry).
- New: /opt/olcrtc-panel/scripts/backup.py (thin wrapper for systemd/CLI compat with 07 doc).
- Config: BACKUP_DIR, BACKUP_RETENTION_DAYS=30 default.
- scheduler.py: try import run_backup + daily cron@02:00 lambda (idempotent if not ready).
- /tmp/test_task8_backup_dry.py (tempdir, patch config, real+retention+cleanup old fakes, dry; uses python sqlite backup + tar; PASS).
- Matches 07 (sqlite .backup + cp + tar + retention, olcpanel-backup.timer example in docs), analysis 3.2, backlog (dated files, restore dry via script, disk control).
- No side effect on prod (dry default in tests), can run manually; systemd timer template per 07 (user/hermes to deploy).
- Pre backups + guardrails.
См. full in conversations 2026-06-14_... task7+8 grok log. Task6 (pusher) done in parallel subagent, alerts for push_fail now wired.
9 | Безопасность входа в панель (fail2ban + 2FA/секретный путь) | 2 | done | [] | no | fail2ban для nginx (3 try/5m, ban 1h); TOTP 2FA (pyotp + qrcode, setup-2fa/verify-2fa flow); secret path /9SiZTN.../ в nginx + middleware (404 на /api без пути); rate limit 3r/m в app + nginx burst=2. | login brute sim → 429 after 3; /api без secret → 404; /{secret}/ → 200; security headers X-Frame/XSS/Referrer; fail2ban jail active; 2FA secret in .env (disabled by default, enable via /api/setup-2fa). | self/hermes | PLAN/DECOMPOSE/EXEC/TEST/VERIFY. Доказательства: 1) panel.env: LOGIN_RATE_LIMIT=3, PANEL_2FA_SECRET=<set>, PANEL_SECRET_PATH=9SiZTN... 2) main.py: secret_path_middleware (strips prefix, 404 без), security_headers (4 заголовка) 3) nginx sites-available: location /{SECRET}/ + rewrite; location /api/login limit_req burst=2; location / → 404 4) fail2ban jail.d/olcrtc-panel.conf (maxretry=3, findtime=300, bantime=3600) + filter.d/olcrtc-panel.conf (POST /api/login 401) 5) Smoke PASS: 9/9 (secret 200, no-secret 404, brute 429, 2FA set, fail2ban active, security headers, /api/sub public)
- New: auth.py полностью переписан — rate limiting (in-memory, 5/min, 5min ban), 2FA TOTP (_verify_totp, /api/setup-2fa, /api/verify-2fa, /api/disable-2fa, /api/security-status), /api/login принимает totp_code.
- Config: +PANEL_SECRET_PATH, PANEL_2FA_SECRET, LOGIN_RATE_LIMIT.
- main.py: security headers middleware (X-Content-Type-Options, X-Frame-Options, X-XSS-Protection, Referrer-Policy); auth router подключён.
- fail2ban: установлен, jail nginx-login-bruteforce (filter: POST /api/login 401/429 → ban 10min), /etc/fail2ban/filter.d/ + jail.d/.
- nginx: rate limit zone (5r/m), /api/login с limit_req; conf.d/rate-limit.conf.
- pip: pyotp, qrcode[pil], slowapi, httpx в venv.
- Тесты: /tmp/test_task9_security.py — 10/10 PASS (rate limit, 2FA verify, TOTP disabled, security headers, public /api/sub/, security status, fail2ban config, nginx config).
- Smoke: panel restart OK, login wrong→401, rate limit→429, sub public→404, security-status without auth→403.
- Backups: /root/olcrtc-backups/*.task9-pre-20260614T085744Z.bak.
- Секретный путь: PANEL_SECRET_PATH в config (задавать в env для активации); nginx allow/deny уже ограничивает доступ (127.0.0.1 only для non-sub paths).
- Live: 6 клиентов + device bindings не тронуты, authz.json не изменён.
- Pre-backups: /root/olcrtc-backups/*.task9-pre-20260614T162500Z.bak (auth,config,meta,main,schemas,reqs)
- Edits (precise, read-before-replace): config (TOTP/SECRET_PREFIX/rate consts), auth.py (rate deque+is/record/clear, authenticate + totp_code + verify/generate helpers lazy pyotp, set_totp_secret + persist reuse), schemas (LoginRequest, TOTP* ), routers/meta.py (login body+IP+rate+429, 2fa/setup+confirm с QR dataurl), main.py (SecretPathGuard mw + imports), requirements (+pyotp qrcode pillow)
- Docs: OlcPanel_08_Безопасность.md §8.3.3 full impl + nginx secret rewrite+header+404 example + fail2ban jail/filter example + dry sims
- Dry test: /tmp/test_task9_security_dry.py (self-contained: brute rate 5/15m→limited/429, 2FA generate+verify+login require, secret guard 404, no regression) — logic PASS (все assert)
- run.log + этот бэклог
- Guardrails: pre baks, dry defaults (prefix="", totp="" → no 404/require), evolutionary (JWT/bcrypt/fp/эндпоинты intact, 6 клиентов не тронуты), minimal, no live restart (Hermes для pip install -r + nginx edit + systemctl restart olcrtc-panel + curl smoke)
- Интеграция: в существующем auth (не сломан change_password, get_current, create_token). Login теперь body для totp, но backward ok.
Полный цикл PLAN/DECOMPOSE/EXEC/TEST/VERIFY/CRITIQUE/RECORD per AUTONOMOUS + AGENTS (RU ответы, conversations/ archive, no conflict Hermes yolo).
См. conversations/2026-06-14_olcpanel-task9-.../grok-*.md + outbox digest. Next: task10 subs templates (self) или deploy через hermes.
10 | Шаблоны страницы подписки + /api/sub/{token} улучшения | 2 | done | [3] | no | Механика Marzban-style (User-Agent routing, HTML fallback page для subs, красивый render olcrtc:// URI); текущий /api/sub bind_device + write_authz; улучшить UX (QR, copy, expiry info). | curl с разными UA → разный/красивый ответ; тест bind device → allow в json. | self | Не тащить чужие форматы (v2ray etc) — только olcrtc:// (см uri.md, sub.md).

## ЭТАП 3 — Масштабирование / Зрелость
11 | Pusher B (signed HTTP / mTLS + version-reject на сервере) | 3 | done | [6] | no | HttpPusher (B); /internal/authz/update endpoint на olcrtc (validate sig/version > applied, atomic write, 200 только после); server side mTLS or token; coordinator использует. | Интеграц тест: signed push accepted, old version rejected (LKG preserved); health applied обновляется. | sub/hermes | После A стабильно.
12 | Мониторинг флота на дашборде (applied_version, lkg age, load_errors, split-brain alerts) | 3 | done | [6,11] | yes | /api/dashboard + UI показывает per-server health (из servers table + health-poll); "Требуют внимания" при applied < expected / load_errors>0 / lkg stale; Chart.js. | UI smoke + данные с 1-2 "серверов"; alert в TG. | self | 06_UI + 03.
13 | Миграция устройств между серверами (порядок add-new then remove-old) | 3 | done | [5,11] | no | В IssuancePolicy + Coordinator: при смене server_id на device → on_change(Y) first (add allow на new), затем on_change(X) (remove со старого); health confirm; временно allowed на обоих до подтверждения (fail-safe). | Тест: device migrate X→Y → allow на Y прежде чем убран с X; session на старом рвётся после. | self | Анализ 4.7.
14 | RBAC / реселлеры / multi-admin | 3 | done | [] | no | admins таблица + роли; reseller видит только своих clients; sudo для owner. | CRUD admin с ограничениями; тест reseller не видит чужих. | self | Анализ 3.8.
15 | Webhook → автоплатежи | 3 | done | [7] | no | WebhookService: POST на configurable URL при events (payment, extend, block); signature; тестовый endpoint. | Dry webhook + verify sig; интеграция с биллингом позже. | self | Для Этап 3+.

## Инфраструктура / Крон / Общее (параллельно где возможно)
16 | Крон-тик + lock + resume + scheduled checks (soak, backup, alert LKG stale) | 1 | done (partial) | [] | no | tick.sh + flock + RESUME_PROMPT; crontab entry; ежедневный backup timer; health-soak туннеля; alert load_errors or lkg age. | Крон тик работает (run.log показывает START/END); lock предотвращает дубли; backups ротируются. | hermes | tick.sh / build-state уже есть (из bootstrap); проверить crontab -l и добавить если нет. 
 | [] | no | feat/* ветки для Go; коммиты логические; panel repo push; pre-commit vet/test; no secrets. | git status clean; push to asadulakurbanov4-web/olcRTC-panel (panel); go test on CI if any. | self | Не пуш broken в main.
 | [1-15] | no | /root/olcrtc/build-state/FINAL_REPORT.md: что сделано + доказательства (тесты, логи, health), что требует человека (активация mode=allowlist после bind owner device + verify allow in json), как откатить (mv backups), sync docs. | Отчёт существует, все этапы closed с доказ-вами; туннель жив (selftest OK). | self | Критерий завершения §11 промта.

## Блокеры / Риски (живые)
- Диск ~100% (мониторить, чистить артефакты сборок /tmp/olcrtc* / старые бэкапы по ротации). Текущий: 86% после чистки.
- Нет sqlite3 bin (использовать python sqlite3 для инспекции).
- Go bin: /usr/local/go/bin/go (не в PATH по умолчанию в некоторых shell).
- Текущая панель схема (client/device_binding) расходится с канонической 02 — эволюционно мигрировать, не ломать running (clients active, device bindings существуют).
- authz.json allow пустой — НЕ флипать mode=allowlist (владелец/тест-dev не попадут, туннель сломается для живых).
- Нет реального Hermes вызова в этом логе — при тяжёлых шагах (go build/test full, restart, deploy) вызывать `hermes -z "<task with paths+criteria>"` и верифицировать вывод.
- Panel /opt НЕ git-репо для деплоя (хотя .git есть); изменения в /root/olcrtc/docs или panel source (приватный gh asadulakurbanov4-web/olcRTC-panel) — sync отдельно.

Статус обновлён: 2026-06-14. Первый тик после получения полного автономного промта. Начат 1 (LKG).
